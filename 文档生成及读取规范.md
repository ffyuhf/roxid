# 文档生成及读取规范

- 版本：v1.0.0
- 生效日期：2026-08-24
- 生效时间：18-36-00（UTC+8）
- 适用项目：roxid（Ollama 全功能复刻 · llama.cpp 子进程后端）

---

## 第一章 文档命名规则

| 组成部分 | 规则 | 示例 |
|---|---|---|
| 实体名称 | 中文或英文实体名，见名知意 | roxid_系统架构 |
| 文档类型/修改动作 | 计划/总览/接口定义/新增/删除/重构/修正/优化 | _新增_ |
| 日期 | YYYYMMDD | 20260824 |
| 时间 | HH-MM-SS（24小时制，紧接日期，下划线分隔） | 18-36-00 |
| 版本号 | vX.Y.Z，初始 v0.0.1/v1.0.0 起，连续不跳跃 | _v1.0.0 |
| 分隔符 | 全部使用半角下划线，禁止空格/特殊字符/中文标点 | — |
| 作者/关键词 | 写入文档头部元数据区，不进入文件名 | — |

完整文件名示例：`[roxid_系统架构]_总览_20260824_18-36-00_v1.0.0.md`

## 第二章 文档路径与目录结构

```text
项目根目录
├─ 文档生成及读取规范.md          # 本文件
├─ 项目文档/
│  ├─ 01-规范/                   # 制度、模板、检查清单
│  │  ├─ 01-文档规范/
│  │  ├─ 02-代码规范/
│  │  ├─ 03-流程规范/
│  │  ├─ 04-模板库/              # 计划书模板、代码修改文档模板
│  │  ├─ 05-交互记录/            # 询问答复、用户裁决
│  │  └─ 归档/
│  ├─ 02-计划/                   # 计划书
│  │  ├─ 01-按年度/2026/08-八月/
│  │  ├─ 02-按项目/roxid/
│  │  ├─ 03-索引/
│  │  └─ 归档/
│  ├─ 03-设计/                   # 架构、接口、流程、数据模型
│  │  ├─ 01-架构设计/
│  │  ├─ 02-接口设计/
│  │  ├─ 03-数据模型/
│  │  ├─ 04-流程设计/
│  │  ├─ 05-图表资源/
│  │  └─ 归档/
│  └─ 04-模块/                   # 模块变更记录
│     ├─ [模块名称]/
│     ├─ 01-变更日志/
│     └─ 归档/
```

目录命名：数字前缀 + 中文 + 连字符，禁止空格、特殊字符、中文标点。
每份文档在 项目文档/ 内必须有唯一、可追溯的路径。

## 第三章 必填元数据字段清单与格式

每份文档头部必须有元数据区：

| 字段 | 格式 | 必填 |
|---|---|---|
| 版本 | vX.Y.Z | 是 |
| 日期 | YYYY-MM-DD | 是 |
| 时间 | HH-MM-SS（UTC+8，24小时制） | 是 |
| 作者 | 生成者标识 | 是 |
| 状态 | 草稿/待审批/已生效/已作废 | 是 |

代码修改文档额外必填：修改类型（新增/删除/重构/修正/优化）、涉及文件清单。

## 第四章 版本号规则与变更日志模板

1. X 主版本：重大变更、不兼容修改；Y 次版本：功能新增、兼容修改；Z 修订：Bug 修复、文档修正。
2. 版本号必须连续，禁止跳跃；初始从 v0.0.1（或首次定稿 v1.0.0）开始。
3. 每次更新必须写变更日志章节，格式：

```markdown
## 变更日志
| 版本 | 日期 | 时间 | 作者 | 变更内容 | 变更原因 | 影响范围 |
|---|---|---|---|---|---|---|
| v1.0.0 | 2026-08-24 | 18-36-00 | Zoo | 初版创建 | 项目启动 | 全部 |
```

## 第五章 文档模板清单

| 模板 | 路径 | 用途 | 更新频率 | 负责人 |
|---|---|---|---|---|
| 计划书模板 | 01-规范/04-模板库/[计划书模板].md | 新建计划书 | 按需 | 生成者 |
| 代码修改文档模板 | 01-规范/04-模板库/[代码修改文档模板].md | 每个修改单元 | 每次修改 | 生成者 |
| 系统架构总览 | 03-设计/01-架构设计/ | 架构基线 | 重大变更时 | 生成者 |
| 变更日志汇总 | 04-模块/01-变更日志/ | 全局变更追踪 | 每里程碑 | 生成者 |

## 第六章 文档读写流程与检查清单

必读流程（编码前置条件）：
1. 读根目录【文档生成及读取规范.md】全文；
2. 读 项目文档/02-计划/ 当前项目计划书（含全部用户裁决）；
3. 读 03-设计/ 中与本任务相关的设计文档；
4. 读 04-模块/ 中将被触碰模块的最近修改文档。
强制阈值：上述任一存在而未读即编码，视为无效操作（回退 S0）。

写入流程：
1. 按第二/三章确定路径与元数据 → 2. 套用第五章模板 →
3. 自查检查清单 → 4. 更新 01-变更日志 → 5. 归档旧版本至 归档/（禁止删除）。

检查清单：
□ 路径正确 □ 命名合规 □ 元数据齐全 □ 章节完整 □ 变更日志已更新
□ 旧版已归档 □ 与代码实际一致 □ 用户裁决已引用原文

## 第七章 质量标准与失效条件

质量标准：清晰（无二义表述）、完整（章节与元数据齐全）、可追溯（裁决引用原文+时间）、可审计（变更日志连续）、与代码一致（无过时/矛盾/遗漏）。

失效条件（任一命中即文档无效，立即整改）：
1. 与代码实际行为矛盾且未标注覆盖关系；
2. 元数据缺失或版本跳跃；
3. 未归档旧版直接覆盖；
4. 将用户未确认事项写成既定事实；
5. 超过两个里程碑未同步更新且内容已失真。

## 变更日志

| 版本 | 日期 | 时间 | 作者 | 变更内容 | 变更原因 | 影响范围 |
|---|---|---|---|---|---|---|
| v1.0.0 | 2026-08-24 | 18-36-00 | Zoo | 初版创建 | M1 文档体系落地（用户确认 2026-08-24 18:26 Q6） | 全部文档 |
